#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit health checks..."

# Run health-check which includes typecheck, lint, and prettier
pnpm health-check

# Run lint-staged to format changed files
echo "🧹 Running lint-staged..."
pnpm lint-staged

echo "✅ Pre-commit checks passed!"
